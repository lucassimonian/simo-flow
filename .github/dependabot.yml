version: 2
updates:
  # Keep GitHub Actions versions current — these are always-safe to bump and
  # clear runner deprecations automatically.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
  # Python deps are pinned and tested deliberately (reproducibility is part of
  # the pitch), so they're bumped by hand after verification — not auto-PR'd.
